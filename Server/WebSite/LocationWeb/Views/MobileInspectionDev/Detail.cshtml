@model DbModel.Location.Work.MobileInspectionDev
@using DbModel.Location.Work

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <style>
        span {
            display: inline-block;
            width: 200px;
            text-align: right;
        }
    </style>
    <title>DetailContentDev</title>
</head>
<body>
    <h2>DetailContentDev</h2>
    <form>
        <fieldset>
            <legend>基本信息</legend>
            <input type="hidden" name="DevID" value=@Model.DevID />
            <span>设备名称：</span><input type="text" name="Name" value=@Model.Name readonly="readonly" /><br />
            <span>设备名称：</span><input type="number" value=@Model.MobileInspectionContents.Count readonly="readonly" /><br />
        </fieldset>
        <fieldset>
            <table class="table">
                <caption><h2 style="text-align:left">巡检内容</h2></caption>
                <tr>
                    <th>顺序</th>
                    <th>内容</th>
                </tr>
                @foreach (MobileInspectionContent item in Model.MobileInspectionContents)
                {
                    <tr>
                        <td>@item.nOrder</td>
                        <td>@item.Content</td>
                    </tr>
                }
            </table>
        </fieldset>
        <hr />
    </form>
    <p>
        @Html.ActionLink("BackToList", "Index")
    </p>
</body>
</html>
