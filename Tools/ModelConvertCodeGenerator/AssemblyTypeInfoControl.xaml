<UserControl x:Class="ModelConvertCodeGenerator.AssemblyTypeInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModelConvertCodeGenerator"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="comboBox"  SelectionChanged="comboBox_SelectionChanged" Grid.Row="1"/>
        <ListBox x:Name="listBox"  Grid.Row="2" />
        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="TextBlock" Grid.Row="0"/>

    </Grid>
</UserControl>
